@model eLibrary.Model.SubjectArea

@{
    ViewBag.Title = "Create";
}

<h4>CREATE A SUBJEC AREA</h4>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Subject Area Creation</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.TheSubjectArea)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TheSubjectArea)
            @Html.ValidationMessageFor(model => model.TheSubjectArea)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
