@model eLibrary.Model.UserPhoto

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>UserPhoto</legend>

        @Html.HiddenFor(model => model.UserPhotoID)

        <div class="editor-label">
            @Html.LabelFor(model => model.LibraryUserID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LibraryUserID)
            @Html.ValidationMessageFor(model => model.LibraryUserID)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
